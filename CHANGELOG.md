# Changelog

## Unreleased

- reframed the repository as a company-neutral workflow governance artifact
- aligned public documentation with the supported packaged runtime under `src/agentic_os/`
- updated install flow to install the local package and use the `agentic-os` CLI
- refreshed the landing page to emphasize policy, approvals, auditability, and operational visibility
- removed provider-specific branding and tool-specific traces from public-facing docs where practical
- updated tests to target the packaged runtime modules
